specVersion: "v1beta"
serviceId: "workout"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "workout-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
